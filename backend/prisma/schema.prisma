generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Drone {
  id          Int    @id @default(autoincrement())
  name        String
  description String
  details     String
  imageUrl    String
  country     String
  type        String
}